'use client'; import React, { useState } from 'react';
import { MRP_PROJECTS } from '@/lib/data';
import { Search, Layers } from 'lucide-react'; export default function MRPProjectsPage() { const [search, setSearch] = useState(''); const filtered = MRP_PROJECTS.filter(p => { return p.client.toLowerCase().includes(search.toLowerCase()) || p.scope.toLowerCase().includes(search.toLowerCase()) || p.process.toLowerCase().includes(search.toLowerCase()); }); return ( <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12 space-y-8 transition-colors duration-200"> <div className="space-y-3 border-l-4 border-[var(--accent-brass)] pl-4"> <div className="inline-flex items-center gap-2 px-2.5 py-1 bg-[var(--bg-secondary)] text-[var(--accent-brass)] text-xs font-semibold uppercase tracking-wider border border-[var(--border-color)]"> <Layers className="w-3.5 h-3.5 text-[var(--accent-steel)] dark:text-[var(--accent-brass)]" /> Official Credentials Data </div> <h1 className="text-3xl font-bold text-[var(--text-primary)]">Metal Recovery Plant (MRP) Projects</h1> <p className="text-xs text-[var(--text-secondary)] max-w-2xl"> Detailed experience table of Metal Recovery Plant (MRP) BOOT, Turnkey, and Jig Machine Supply projects executed by Venkateswar Engg Works Pvt. Ltd. </p> </div> <div className="bg-[var(--bg-surface)] p-4 border border-[var(--border-color)] flex justify-between items-center text-xs"> <div className="relative w-full sm:w-80"> <Search className="w-4 h-4 absolute left-3 top-1/2 -translate-y-1/2 text-[var(--text-muted)]" /> <input type="text" placeholder="Search by client, scope or process..." value={search} onChange={e => setSearch(e.target.value)} className="w-full pl-9 pr-3 py-2 bg-[var(--bg-secondary)] border border-[var(--border-color)] text-[var(--text-primary)] placeholder-[var(--text-muted)] focus:outline-none focus:border-[var(--accent-steel)]" /> </div> </div> <div className="industrial-table-container custom-scrollbar"> <table className="industrial-table"> <thead> <tr> <th>Client Name</th> <th>Scope</th> <th>Type</th> <th>Installation / O&M Period</th> <th>Process</th> <th>Remarks & Performance</th> </tr> </thead> <tbody> {filtered.map((row, idx) => ( <tr key={idx}> <td className="font-bold text-[var(--text-primary)]">{row.client}</td> <td className="text-[var(--accent-steel)] dark:text-[var(--accent-brass)] font-semibold">{row.scope}</td> <td> <span className="px-2 py-0.5 bg-[var(--bg-secondary)] text-[var(--accent-brass)] font-bold text-[11px] border border-[var(--border-color)]"> {row.type} </span> </td> <td>{row.period}</td> <td>{row.process}</td> <td className="text-[var(--text-secondary)]">{row.remarks}</td> </tr> ))} </tbody> </table> </div> </div> );
}
